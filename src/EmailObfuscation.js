/*
import React from 'react';

const EmailObfuscation = () => {
    // Define email parts using HTML entities
    const user = '&#121;&#111;&#117;&#114;&#110;&#97;&#109;&#101;';
    const domain = '&#101;&#120;&#97;&#109;&#112;&#108;&#101;&#46;&#99;&#111;&#109;';

    // Combine parts to form the email address
    const email = `${user}@${domain}`;

    return (
        <a href={`mailto:${user}@example.com`} dangerouslySetInnerHTML={{ __html: email }} />
    );
};

export default EmailObfuscation;*/
